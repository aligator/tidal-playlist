---
id: A-11
severity: INFO
area: Architecture
status: Open
---

# A-11 · Three legacy components are entirely dead — 637 LOC to delete

**Files:**
- `web/src/components/app-toolbar.ts` — 150 LOC, all native `<button>`, pre-Lit toolbar
- `web/src/components/selected-songs-panel.ts` — 213 LOC, raw `<table>` + custom action buttons
- `web/src/components/list-manager.ts` — 474+ LOC, custom dropdown with raw `<input>`

## Description

None of the three are imported by any other file in `web/src/`. Closes UX-5.

**Note:** `impressum-modal.ts` is NOT included — see A-15. Impressum is a legal requirement.

## Fix

Delete all three files; remove from bundle.
