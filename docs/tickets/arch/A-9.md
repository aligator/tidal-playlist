---
id: A-9
severity: INFO
area: Architecture
status: Open
discovered: Playwright live session
---

# A-9 · `playlist-settings.ts` is dead code — raw inputs never rendered

**File:** `web/src/components/playlist-settings.ts`

## Description

UX-2 identified raw `<input>` / `<select>` elements here. Playwright testing confirms
the component is **never imported or rendered** — live UI uses `playlist-view.ts` with
proper MWC components. ~400 LOC of stale code confuses future contributors.

## Fix

Delete `web/src/components/playlist-settings.ts`. Close UX-2 as superseded.
