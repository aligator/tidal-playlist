---
id: L-2
severity: LOW
area: Backend
status: Open
---

# L-2 · `flowSigningKey` cached as module-level mutable singleton

**File:** `server/auth/oauth.ts` L38–40

## Description

Safe today. If secret rotation is added, cached key silently continues with old secret.
Caching dependency on secret value is implicit.

## Fix

Make caching dependency on secret value explicit (e.g. cache key includes secret hash).
