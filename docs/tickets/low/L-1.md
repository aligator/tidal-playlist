---
id: L-1
severity: LOW
area: Frontend
status: Open
---

# L-1 · `parseJwtExpiry` parses JWT without signature verification

**File:** `web/src/modules/tidal/shared.ts` L52–64

## Description

Client-side pattern — browser cannot verify server-signed JWTs. Acceptable, but
the intentional limitation should be documented to prevent future misuse.

## Fix

Add an explicit comment noting that expiry parsing only, no signature verification.
