---
id: M-8
severity: MEDIUM
area: Frontend/CSP
status: Done
discovered: Playwright live session
---

# M-8 · `connect-src` missing `https://api.tidal.com` — TrueTime blocked on load

**File:** `server/main.ts` L37

## Description

`@tidal-music/true-time` (pulled in by `@tidal-music/auth`) pings
`https://api.tidal.com/v1/ping` on startup to synchronise clock offset. This domain
is not in `connect-src`. The browser blocks the request:

```
TypeError: Failed to fetch  (index-DTp7haYV.js:929)
```

on every page load. TrueTime falls back to `Date.now()`, degrading token-expiry
accuracy. Error pollutes the console making other errors harder to spot.

## Fix

Add `https://api.tidal.com` to `connect-src`.
